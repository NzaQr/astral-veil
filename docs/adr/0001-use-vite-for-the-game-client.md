# Use Vite for the game client

The first release is a client-rendered local game with no profiles, history, SEO, or application server endpoints. We will use React with Vite instead of the originally proposed Next.js so the game can ship as static assets; adopting SSR or a full-stack framework would add complexity without serving a current requirement.
